# 绿联 LED 灯控 v1.5.0 → v1.7.6：新增 BIOS 控制、多机型适配与盘位校准

这段时间，绿联 LED 灯控不只是多了几种灯效，而是从基础的 LED 管理，升级到了**多机型硬件控制工具**。

其中最重要的一项是：**支持在 fnOS 网页里直接读取和控制部分机型的 BIOS 硬件能力。**

- 最新版下载：[v1.7.6 Release](https://github.com/BearHero520/LLLED_FPK/releases/tag/v1.7.6)
- 项目地址：[LLLED_FPK](https://github.com/BearHero520/LLLED_FPK)

## 本轮新增了什么？

### 1. 新增 BIOS 硬件控制：不只是控制灯

v1.7.6 直接集成 [UGREEN-NAS-Hardware 的 `ugreenctl`](https://github.com/BearHero520/UGREEN-NAS-Hardware)，在支持的设备上可在应用网页内读取 BIOS 硬件状态，并进行实际控制：

- 查看 CPU、系统风扇的**转速、PWM 值和自动/手动模式**；
- 调节风扇 PWM，并切换自动/手动控制；
- 设置**来电启动策略**：来电开机、保持关闭或恢复上次状态；
- 风扇模式和 PWM 调整后**立即写入硬件**，无需再点一次“应用”，页面会同步显示写入结果。

目前 BIOS 控制适用于 **DXP4800 Plus、DXP4800 Pro、DXP480T Plus**。其中 DXP480T Plus 已支持读取三路风扇状态；为保证硬件安全，写入项会按机型限制开放。

> 这意味着应用已经从“机箱灯光控制”进一步延伸到 NAS 的散热与开机策略管理。

### 2. 新增多机型档案与多种控制后端

从 v1.6.8 开始，应用引入多机型档案：自动通过 DMI 识别机型，也支持手动选择。不同机型会使用匹配的协议、LED 编号、盘位数量与 HCTL 映射规则。

同时增加多条控制路径：

- 内置 `ugreen_leds_cli`；
- 实验性 `led-ugreen` sysfs / DKMS 驱动；
- DXP480T 系列专用的 N76E003 电源灯后端。

应用会检查后端状态，并避免多个控制路径同时占用 I²C，驱动不可用时可安全回退到内置 CLI。

### 3. 新增 2 / 4 / 6 / 8 盘位映射与盘位灯校准

从 v1.5.0 开始，新增 6/8 盘位 HCTL 与 LED 映射；v1.5.1 又补齐了“实验室”校准功能。

当硬盘实际位置和机箱灯顺序不一致时，可以逐灯识别后保存规则：

- **按位置绑定**：灯跟随物理盘位，适合固定机箱位置；
- **按硬盘绑定**：灯跟随硬盘序列号，换槽后仍能识别同一块盘；
- 可随时恢复自动 HCTL 映射。

这解决了多盘位设备常见的“盘 3 在读，结果亮的是盘 5”的问题。

### 4. 新增 DXP480T / DXP480T Plus 专属灯光支持

DXP480T 系列使用独立控制器，和普通 RGB 灯控机型不同。v1.6.9 起应用提供了专属后端和专用页面：

- 控制红/白电源灯；
- 支持常亮、快闪、慢闪、呼吸和关闭；
- 可让电源灯根据网络活动闪动；
- 自动使用专用控制方式，不会误加载不适用的通用 LED 驱动。

### 5. 完善升级、诊断和安全回退

- 网页可显示机型档案、协议、控制后端、DKMS 与内核 headers 状态；
- 发现新版时可查看更新说明并下载 FPK；
- LED 硬件或依赖暂时不可用时，应用仍可完成安装并给出诊断信息；
- 实验性驱动需用户明确确认后才会安装，不会静默改动系统环境。

## 当前支持情况

| 状态 | 机型 |
|---|---|
| 已验证 | DX4600 Pro、DX4700+、DXP2800、DXP4800、DXP4800 Plus、DXP6800 Pro、DXP8800 Plus |
| 实验性 | DXP4800 GT、iDX6011、iDX6011 Pro、iDX6012 |
| 待验证 | DXP2800 GT、DXP4800 Pro |
| 受限支持 | DXP480T / DXP480T Plus：红/白电源灯；其中 DXP480T Plus 还支持 BIOS 风扇与来电启动控制 |

## 后续计划：继续扩展更多绿联机型

接下来会持续补充机型档案、盘位映射和硬件控制能力，优先推进更多绿联 NAS 的实机验证与适配。也欢迎大家反馈尚未覆盖的型号、盘位灯顺序、fnOS 版本和测试现象——真实设备反馈会直接用于完善后续支持。

如果你的 NAS 已在支持列表内，可以直接安装最新版；如果暂未覆盖，也欢迎留言报机型，一起把支持范围做得更完整。

> 安装要求：fnOS 0.9.27 及以上、x86 平台。请不要同时运行其他会占用相同 LED/I²C 控制器的工具。

项目采用 AGPL-3.0-only 开源，欢迎 Star、反馈和参与测试。
